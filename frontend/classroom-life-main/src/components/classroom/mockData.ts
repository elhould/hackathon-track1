export const mockDialogue = {
  student: "How do the plates actually move?",
  tutor: "Great question! Let's explore that!"
};

export const mockLearningContent = {
  title: "Plate Tectonics",
  formula: "Continental Drift Theory",
  example: "Pangaea → Today's Continents",
  imageUrl: "/placeholder.svg"
};

export const mockLessonInfo = {
  subject: "Geography",
  topic: "Plate Tectonics",
  greeting: "Welcome Class!",
};

export type DialogueData = typeof mockDialogue;
export type LearningContent = typeof mockLearningContent;
export type LessonInfo = typeof mockLessonInfo;
